import React from 'react';
import Auxiliary from './Auxiliary';

const Protected = (props) => {
  return (
    <Auxiliary>
      <h1>Protected</h1>
    </Auxiliary>
  );
};

export default Protected;
